# .NET
bin/
obj/
*.user
*.suo
*.vs/

# Node
node_modules/
dist/
.vite/

# Environment
.env
*.env.local

# IDE
.idea/
.vscode/
.cursor/
*.swp
*.plan.md

# OS
.DS_Store
Thumbs.db

# Build
*.log

# TeslaMate
# ExempleTeslaMateRepo/

# Tesla upstream sources kept locally for reference, never committed
vehicle-command-src/
fleet-telemetry-src/

# 3d tests
Tesla-APK-Android/
Tesla-Godot-Test/
docs/3d-viewer-spec.md

# Tesla 3D models (proprietary assets, never commit). These are the
# Tesla-app GLBs operators may mount at runtime via ./teslahub-models.
src/TeslaHub.Web/public/models/*.glb
community-m3-rigged-trappe.blend
community-m3-rigged.glb.old
# NOTE: the bundled CC-BY community model lives in public/community-models/
# (committed, shipped in the image) — NOT here — so it's never shadowed by
# the optional /srv/models volume. Credit: ChoochooLi (CC BY 4.0).